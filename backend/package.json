{
  "name": "studenhub-backend",
  "version": "1.0.0",
  "description": "StudenHub backend — Express + Turso (LibSQL) + JWT auth",
  "main": "server.js",

  "engines": {
    "node": "20.x"
  },

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },

  "dependencies": {
    "express": "^4.18.2",
    "@libsql/client": "0.5.6",    
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5"
  },

  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
